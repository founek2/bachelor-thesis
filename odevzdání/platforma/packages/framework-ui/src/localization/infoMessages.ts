import { messageFactory } from './index';
import * as types from '../types';

export const messages: types.messages = {
    noneChangeFound: 'Nebyla nalezena žádná změna',
};

export default messageFactory(messages)
